/**
 * Auth hook — exposes the current user, role, and login/logout actions.
 * Built out in Phase 1 alongside the backend's Google OAuth endpoint.
 * Other hooks (useRole, useNotifications) are added the same way in
 * their respective phases, each in its own file under src/hooks/.
 */

// TODO(Phase 1): implement useAuth (Google OAuth login, JWT storage,
// current user context).
export {};
