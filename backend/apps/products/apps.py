from django.apps import AppConfig


class ProductsConfig(AppConfig):
    """Product master data. Built out in Phase 2."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.products"
