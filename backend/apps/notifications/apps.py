from django.apps import AppConfig


class NotificationsConfig(AppConfig):
    """In-app notification badges. Built out in Phase 5."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.notifications"
